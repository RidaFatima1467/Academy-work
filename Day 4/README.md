1. Learn about Drop and Dropna.
2. then check how ro change the dtype of any column.
3. apply and vectorized operation.
4. pivot and pivot_table difference.
5. merge operation and its 4 types *inner join *left join *right join *outter join
6. implementation task:
 * load a amazon sales dataset.
 * check the missing values and then handle them.
 * check the datatypes od all the columns and change datatypes by using astypes as necessary.
 * check duplicate rows and delete them.
 * add 2 columns in th dataset. grosssales or discountamount
 * create a small dataframe and merge it into a dataset.